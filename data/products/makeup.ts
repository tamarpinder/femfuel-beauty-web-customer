import { Product } from "@/types/product"

export const makeupProducts: Product[] = [
  // Products removed - duplicates exist in featured.ts
  // These products should only exist in featured collection
]